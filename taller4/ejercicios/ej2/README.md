# Ejercicio 2 — Overflow

Completar `overflow_4b.sv` para complemento a dos y conectar el detector al sumador provisto en
`top_module.sv`, con `cin=0`.

Ejecutar `make sim` y verificar que los 256 casos pasen. No modificar
el testbench. En HDL Studio usar `top_module` como top y agregar todos
los `.sv` locales salvo el testbench. Ver `../../README.md`.
