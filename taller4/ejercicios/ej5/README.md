# Ejercicio 5 — Zero

Completar `zero_4b.sv`: recibe `dato` de 4 bits y devuelve `zero`.
No utiliza un sumador. ¿Depende de la representación?

Ejecutar `make sim` y verificar que los 16 casos pasen. No modificar
el testbench. En HDL Studio usar `zero_4b` como top y agregar todos
los `.sv` locales salvo el testbench. Ver `../../README.md`.
